[build]
builder = "RAILPACK"

[deploy]
startCommand = "php -S 0.0.0.0:${PORT:-8080} -t backend/public backend/router.php"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
